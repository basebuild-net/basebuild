node_modules/
dist/
.vite/
# Cargo build output — the default `target/` plus any alternate CARGO_TARGET_DIR
# used for isolated check/test runs while the app holds a lock on `target/`.
src-tauri/target*/
*.log
.DS_Store
.bb-screenshots/
.env
.env.*
.env.local
screenshots/test-results/
playwright-report/
test-results/
artifacts/

# OpenSpec plans are local workspace state, never repository content.
/openspec/

# Scratch todo/notes files — local only, cleaned via `rm -rf tmp/`.
/tmp/